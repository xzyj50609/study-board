# REQUIREMENTS · 需求说明

版本：v0.1（待你确认）
来源：你的说明书（十一节）+ Claude Design 交付（`交接说明.md` / `交互原型.dc.html` / `设计系统.dc.html` / `design-tokens.json`）
两者冲突处一律以「你的说明书」为准，冲突清单见 `DESIGN_AUDIT.md` 7.1 与 `OPEN_QUESTIONS.md`。

标记约定：**[待确认]** = 依赖 `OPEN_QUESTIONS.md` 里的裁决，代码里要留出口子但不先实现。

---

## 1. 产品目标

一个只给你自己用的、离线的考研英语文章学习打卡 App。

它要回答三个问题，别的都不做：
1. **今天该做什么** —— 当前是第几篇、第几天、还剩哪几项。
2. **我走到哪了** —— 252 个小任务完成了多少、42 篇完整读完了几篇。
3. **我是领先还是落后** —— 提前做的存成额度，漏做的显示缺口，哪天该补一目了然。

设计基调是"夜读 / Ritual"：深墨底、暖砂字、月光蓝表示已完成、暖金表示提前、暗铜表示缺额。它是台灯不是仪表盘，不用徽章、彩带、排行榜制造兴奋。

## 2. 功能范围

### 第一版做

1. 首次设置向导：起点、总篇数、基础节奏、计划开始日期
2. 今日学习：任务勾选、整体进度环、当前文章进度、额度/缺额提示、主按钮引导下一项
3. 文章任务详情：当前篇 6 项全列、单项时间戳
4. 月历：一格一天、9（或 10）种状态、月份切换、本月摘要
5. 日期任务详情面板：查看某天的原计划与实际完成、就地补记或撤销
6. 整体进度页：百分比、双段进度条、42 篇矩阵、预计完成日、历史记录
7. 设置页：改节奏、重进向导、重新排期、撤销整篇
8. 全部完成页
9. 撤销 / 补打卡 / 重新排期三条修改历史的路径
10. 全部数据本地保存（Room + DataStore），无账号、无网络
11. **备份导出与恢复**：导出一份 JSON 到你选的位置，也能从 JSON 恢复（2026-08-06 定进第一版）

### 第一版不做（明确排除）

**每日提醒通知**（2026-08-06 定，Q1）、**每周学习日 / 休息日**（2026-08-06 定，Q2，天天都是学习日，靠额度休息）、登录注册、云同步、社交、排行榜、付费、成就徽章、单词/短语/长难句的内容录入、错题原因记录、文章原文、浅色主题、桌面小组件、多设备、平板/横屏专门适配、WebView 套壳。

## 3. 核心概念（先把词统一了）

| 词 | 定义 |
|---|---|
| **文章 / 篇** | 42 篇之一，编号 1–42 |
| **小任务** | 一篇里的 6 项之一，编号 1–6，全 App 共 42 × 6 = 252 项 |
| **第一天 3 项** | ①通读全文 ②完成课后习题 ③核对答案，标出错题和不确定的题 |
| **第二天 3 项** | ④弄懂错题和不确定的题 ⑤弄懂标出的单词和短语 ⑥弄懂标出的长难句 |
| **计划日** | 从计划开始日起算的学习日；默认每篇 2 个计划日、每个计划日 3 项 |
| **相位** | 一篇内的第一天 / 第二天，对应任务 1–3 / 4–6 |
| **当前篇** | 从起点篇往后，第一个不满 6 项的篇 |
| **额度 (credit)** | 已完成项数减去按计划本应完成的项数。正数＝领先（可休息的天数），负数＝缺额（要补的项数） |
| **补打卡** | 给过去某天补记完成 |
| **重新排期** | 把剩余计划整体挪到从今天开始，额度/缺额归零，历史记录不动 |

任务名称固定为上面 6 句，不可编辑。

## 4. 页面功能

### 4.1 首次设置向导 `setup`

首次启动的起始页；之后可从设置页"调整起点与总篇数"再进入。

字段：

| 字段 | 控件 | 默认值 | 说明 |
|---|---|---|---|
| 文章总数 | 数字（原型里写死 42） | 42 | **[待确认 Q3]** 是否允许改 |
| 已经学完 | −/+ 步进器，0–41 | 12 | 改它会实时更新"下一篇是第 N 篇" |
| 下一篇编号 | 派生显示 | 已学完 + 1 | **[待确认 Q3]** 是否允许与"已学完"解耦 |
| 计划开始日期 | 日期选择 | 今天（北京时间） | **[待确认 Q3]** 设计里没有这一项 |
| 基础节奏 | 三选一 | 2 天/篇 | 1 / 2 / 3 天每篇；只影响排期和额度，不锁定操作 |

（每周学习日与提醒时间已定为第一版不做，向导里不出现这两项。）

实时预览卡：「剩余 N 篇 · 基础计划 M 个学习日 · 预计 X 月 X 日完成」。

主按钮：「开始第 N 篇」→ 写入计划 → 进 today。

**再次进入向导时**：保留已有的完成记录，只改计划参数（见 6.4 的重算规则）。

### 4.2 今日学习 `today`（App 起始页）

从上到下：

1. 日期行：`8 月 5 日 周三 · 14:32 北京` + 右上角"设置"文字入口
2. 标题：一句人话（规则见 5.6）
3. 主区：132dp 进度环（蓝弧=已完成、金弧=额度、空=剩余，中心显示 `28.57%` 和 `72 / 252`）+ 右侧「完整学完 12/42 篇」和「当前第 13 篇」6 段刻度 + `0/6 项 · 0.00%`
4. 额度卡（三选一）：
   - 额度 > 0：金卡，`+2`，"你已经领先计划 2 天"，副标题给原定完成日和现在的预计完成日
   - 额度 = 0：中性卡，"正好跟上计划"
   - 额度 < 0：暗铜卡，"额度已用完，缺 N 项"，副标题给最早缺口日期，带两个按钮「优先补上」「重新排期」
5. 「第一天 · 3 项」分组（是今日计划时标注"（今日计划）"）+ 3 个任务行
6. 「第二天 · 3 项」分组（不是今日计划时标注"（可提前）"）+ 3 个任务行
7. 底部提示一句
8. 固定底部主按钮：「完成「通读全文」」/「开始第 N+1 篇」/「查看完成页」（规则见 5.7）
9. 底部导航

任务行点击行为：未完成 → 直接记完成；已完成 → 弹撤销确认。

### 4.3 文章任务详情 `article`

从 today 的"文章详情"进入。展示当前篇的 6 项（不再分第一天/第二天两组，但顺序不变）、大号 `0/6`、当前文章进度条、每项的完成时间戳或"未完成"、以及一句"计划 X 月 X 日起两天完成。顺序不限……"的说明。交互与 today 的任务行一致。

### 4.4 月历 `calendar`

- 顶部固定：月份 + 年 + 前后月箭头（也支持横滑切月）
- 摘要三格：本月完成 N 项 / 剩余额度 N 天 / 需补 N 项
- 星期表头：一 二 三 四 五 六 日（周一起始）
- 日格：日期数字 + 11dp 状态光点 + 8sp 的「篇·第几天」（如 `13·¹`）
- 图例
- 点任意日格 → 底部面板；长按 → 直接进编辑（**[待确认 Q5]**：长按在原型里没实现，建议第一版先只做点击）

日格状态 9 种：计划中 / 今天 / 按计划完成 / 部分完成 / 后续补完成 / 提前完成 / 额度覆盖 / 缺额待补 / 计划开始前。判定规则见 5.5。（无休息日态。）

### 4.5 日期任务详情面板 `daySheet`

底部弹出，默认 60% 高，可拖到全屏。内容：日期 + 星期、"原计划 · 第 N 篇 第一天 · 3 项"、右上状态标签、当天计划的 3 项（可勾/可撤销，带时间戳）、一句随状态变化的说明、底部「关闭」+「全部标记完成」。

落在计划开始之前的日期：显示空态，隐藏"全部标记完成"（原型这里有 bug，见 `DESIGN_AUDIT.md` 7.2.3）。

### 4.6 整体进度 `progress`

60sp 百分比 → 双段进度条 → `72 / 252 项 · 12 / 42 篇` 和额度注记 → 42 格文章矩阵（实心=6/6、半亮=进行中、空=未开始）→ ETA 卡（基础计划完成日 / 按当前速度预计 / 一句解释 / 「根据当前进度重新排期」按钮）→ 历史记录列表。

历史记录：时间戳 + 「第 N 篇 · 任务名」+ 标签（提前 / 完成 / 补记 / 撤销 / 排期）。空态文案："还没有记录 · 勾掉第一项就会出现在这里"。

**[待确认 Q3]** 起点的 12 篇（72 项）不进历史列表——它们没有真实完成时间，列出来只会是一堆假时间戳。

### 4.7 设置 `settings`

- 基础节奏（2 天/篇 ›）
- 时区（Asia/Shanghai，第一版只读展示，不可改）
- 调整起点与总篇数 → 重进向导
- 重新排期 → 确认弹窗
- 撤销第 N 篇的完成 → 确认弹窗
- **导出备份** → 系统文件选择器，存一个 JSON
- **从备份恢复** → 系统文件选择器选 JSON → 校验 → 二次确认（会覆盖现有数据）→ 恢复
- 说明文字："数据仅保存在本机，不联网。42 篇 × 6 项 = 252 项。"

备份两行按现有卡片样式补（设计没画这一块，我按同一套 token 加，不引入新样式）。
- 原型里的"DEMO · 演示状态"四个按钮**不实现**

### 4.8 全部完成页 `done`

252/252 时替换 today 的内容，全屏无底栏：150dp 金环 + "42"、"42 篇全部读完"、"252 / 252 项 · 100.00%"、"用了 N 天，比基础计划提前 M 天"、「查看全部记录」按钮跳 progress。

## 5. 业务规则（可测试的形式）

编号规则可以直接一条对一个单元测试。

### 5.1 计划生成

- R1：第 k 个计划日（k 从 0 开始）对应 `篇号 = 起始篇 + floor(k / 每篇天数)`，`相位 = k mod 每篇天数 + 1`。
- R2：默认每篇 2 天，相位 1 → 任务 1,2,3；相位 2 → 任务 4,5,6。
- R3：计划日 = 自然日，没有休息日，不跳过任何一天（2026-08-06 定）。休息通过额度实现：提前做出来的额度天数就是可以不学的天数。
- R4：每篇 1 天时 6 项全排在一天；每篇 3 天时按 2/2/2 拆分（**这条设计和你的说明书都没写死，我按均分处理**）。
- R5：计划只算到第 42 篇的最后一项，之后不再排。

### 5.2 进度计算

- R6：整体进度 = 已完成小任务数 / 252，保留两位小数（`"%.2f"`，28.57%）。
- R7：完整篇数 = 6 项全完成的篇数；第 6 项勾上的瞬间 +1，撤销任意一项立即 −1。
- R8：当前文章进度 = 该篇已完成项 / 6。
- R9：当前篇 = 从起点篇往后第一个不满 6 项的篇；全部满则为第 42 篇。
- R10：整体进度**只反映真实完成数**。设计里写"整体进度只增不减"指的是正常使用下的观感，但撤销必须让它减——你的说明书第七节明确要求撤销后重算，以说明书为准。

### 5.3 额度与缺额

- R11：应完成项数 = `已过去的计划日数 × 每日项数`，其中"已过去"**不含今天**（今天的任务当天不算欠）。默认每日 3 项。
- R12：额度（项）= 计划开始后新完成的项数 − 应完成项数。
- R13：额度天数 = 额度项数 / 3，正数保留一位小数（`+1.5 天`），整除时不显示小数。
- R14：额度 > 0 → 金色领先态；= 0 → 中性齐平态；< 0 → 暗铜缺额态，缺额项数 = |额度|。
- R15：**起点的已学完篇数不参与额度计算**，只进整体进度。额度只看计划开始日之后产生的记录。
- R16：最早缺口日期 = 从计划开始日正向扫描，第一个"当日计划的项没有全部完成"的过去计划日。（不是原型里的反推估算。）
- R17：预计完成日 = 今天 + `ceil(剩余项 / 3)` 天。
- R18：基础计划完成日 = 计划开始日 + `(42 − 起始篇 + 1) × 每篇天数 − 1`。

### 5.4 完成、撤销、补打卡

- R19：勾选一项 → 写一条记录，记录里存**实际完成时刻**和**当时这一项的计划日期快照**。
- R20：撤销 → 删除该记录，并在历史里追加一条"撤销"事件。撤销必须先弹确认，确认文案要写出回退后的两个数字（整体项数、完整篇数）。
- R21：撤销整篇 → 一次删除该篇 6 条记录，确认文案同样写出回退结果。
- R22：补打卡（面板里勾过去某天的项）→ 完成时刻记为**实际操作时刻**，计划日期快照记为**该项原本的计划日**，因此它天然被标记为"补记"。
- R23：「优先补上」按最早缺口顺序，一次补齐当前缺额数量的项。
- R24：一天可以完成任意多项，没有上限，不受计划相位限制。
- R25：任务顺序不限，任何一项都可以先勾（原型和设计文案都明确写了这条）。

### 5.5 日历日格状态判定

设 D 为某个日期，P(D) 为该日的计划（篇号 + 相位 + 3 个任务），今天为 T：

- R26：D < 计划开始日 → **计划开始前**（灰蓝点，不可勾）
- R27：（休息日态第一版不做，编号保留占位）
- R28：D == T：当日完成 3 项 → 按计划完成；当日完成 1–2 项 → 部分完成；0 项 → **今天**（外环）
- R29：D < T：
  - 当日实际完成了该日全部 3 项 → **按计划完成**
  - 当日实际完成 1–2 项 → **部分完成**
  - 当日没完成，但这 3 项后来被补上了 → **后续补完成**（空心蓝环）
  - 这 3 项在 D 之前就已完成 → **提前完成**（金环）
  - 3 项没全完成，但当前额度 ≥ 0（被别处的提前量覆盖）→ **额度覆盖**（淡金）
  - 3 项没全完成且额度 < 0 → **缺额待补**（暗铜 + 格底）
- R30：D > T：该日 3 项已全部完成 → **提前完成**（金环）；否则 → **计划中**

（注：R29 里"后续补完成"和"提前完成"在原型里被合并处理了，设计系统第 06 节把两者分开画了两个态。我按设计系统实现，取记录时间戳与计划日期比较得出。）

### 5.6 首页标题规则

- R31：全部完成 → "全部读完了"
- R32：当前篇 0 项完成 → "今天开始第 N 篇"
- R33：当前篇 1–5 项完成 → "第 N 篇还剩 M 项"
- R34：当前篇 6 项完成 → "第 N 篇已完成"

**我把原型里的"的第 X 天"从标题里去掉了**，因为逾期时"当前篇"和"计划相位"会指向不同的篇，拼在一句里是错的（`DESIGN_AUDIT.md` 7.2.1）。相位信息保留在下面的分组标签"第一天 · 3 项（今日计划）"里，那里它是准确的。

### 5.7 底部主按钮规则

- R35：全部 252 项完成 → 「查看完成页」
- R36：当前篇 6 项完成 → 「开始第 N+1 篇」（点击后当前篇自然推进，不需要额外状态）
- R37：有缺额时 → 「补上 X 月 X 日的「任务名」」，指向最早缺口的第一项
- R38：其他 → 「完成「最早未完成项的任务名」」

（R37 是我按你说明书第六节"优先展示最早未完成任务"加的，设计原型里主按钮永远指向当前篇的最早未完成项。）

### 5.8 重新排期

- R39：确认后 `计划开始日 = 今天`、`起始篇 = 当前篇`。已完成记录和它们的时间戳、计划日期快照**一律不变**。
- R40：重排后额度归零（领先的额度被"兑现"成更早的完成日，缺额被清账）。
- R41：重排要弹确认，文案区分领先/缺额两种情况，并写出重排后的预计完成日。
- R42：**不自动重排**。提前完成、漏做都不触发自动重排，只有用户主动点才排。

### 5.9 备份导出与恢复

- R47：导出内容 = 计划参数 + 全部完成记录（含 IMPORTED）+ 全部历史事件 + 格式版本号 + 导出时刻。
- R48：格式为 JSON，人能直接看懂；文件名形如 `夜读备份-20260806-2214.json`。
- R49：导出走系统文件选择器（`ACTION_CREATE_DOCUMENT`），由你决定存哪；**不申请任何存储权限**。
- R50：恢复走 `ACTION_OPEN_DOCUMENT`；读入后先校验格式版本与结构，不合法就报错并原样保留现有数据。
- R51：恢复是**整库替换**，不是合并——合并会在"同一项两边都完成但时间不同"时产生无解的冲突。执行前必须二次确认，文案写明"现有数据会被覆盖"。
- R52：恢复成功后所有派生量立即重算，并回到 today。

### 5.10 时间与时区

- R43：所有"今天""日期"判定一律用 `ZoneId.of("Asia/Shanghai")`，与设备时区无关。
- R44：完成时刻按北京时间显示（`08.04 22:14`）。
- R45：App 在前台跨过北京时间 00:00 时，今日计划、额度、日历必须自动重算，不能等下次冷启动。
- R46：从后台回到前台（`ON_RESUME`）时重算一次。

## 6. 用户流程

### 6.1 首次使用
装好 → 向导（默认已学完 12、下一篇 13、2 天/篇、今天开始）→ 「开始第 13 篇」→ today 显示 `72/252 · 28.57%`、当前第 13 篇 0/6。

### 6.2 一次正常的打卡
today → 点「通读全文」→ 圆点 220ms 填充 + 轻触觉 → 主环 480ms 扫过 → 当前文章刻度 +1 → 主按钮变成「完成「完成课后习题」」→ 历史多一条。

### 6.3 提前做
第一天 3 项做完 → 底部提示"还有余力？可以提前做第二天的 3 项" → 继续勾第二天的 3 项 → 该篇 6/6，完整篇数 +1，主按钮变「开始第 14 篇」→ 额度 +1 天，月历上原定的第二天变金环。

### 6.4 漏做后补
空 2 天 → 额度从 +2 掉到 −6（举例）→ today 出现暗铜卡"额度已用完，缺 6 项 · 最早缺口 08.07" → 点「优先补上」按最早缺口顺序补，或去月历点那一天在面板里逐项勾。

### 6.5 改历史
月历 → 点某天 → 面板里勾/取消 → 撤销要过确认弹窗 → 所有派生量立刻重算（整体进度、完整篇数、当前文章进度、额度、预计完成日）。

### 6.6 重新排期
进度页或设置页 → 「根据当前进度重新排期」→ 确认弹窗写清后果 → 确认 → 计划轴从今天重画，历史一条不动。

## 7. 数据字段

### 7.1 任务完成记录（每条 = 一个小任务的一次完成）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | String | `"13-4"` = 第 13 篇第 4 项，天然唯一 |
| `articleIndex` | Int | 1–42 |
| `taskIndex` | Int | 1–6 |
| `completedAt` | Instant? | 实际完成时刻；起点导入的记录为 null |
| `plannedDate` | LocalDate? | 完成那一刻这一项的计划日期快照；重排/撤销都不改它 |
| `source` | Enum | `CHECKED`（当天勾的）/ `BACKFILL`（补打卡）/ `IMPORTED`（首次设置带入的起点篇） |

派生标记（**不入库**，每次算）：
- 是否提前 = `completedAt.北京日期 < plannedDate`
- 是否逾期完成 = `completedAt.北京日期 > plannedDate`
- 是否为补打卡 = `source == BACKFILL`
- 实际完成日期 = `completedAt` 转北京时区取日期

不入库的原因：这些标记只有跟计划比较才有意义，一旦存成布尔值，将来任何一次重排或历史修改都可能让它们与事实不符。

### 7.2 计划

| 字段 | 类型 | 默认 |
|---|---|---|
| `totalArticles` | Int | 42 |
| `tasksPerArticle` | Int | 6（常量，不开放） |
| `startArticle` | Int | 13（= 已学完 + 1） |
| `completedBeforeStart` | Int | 12 |
| `planStartDate` | LocalDate | 首次设置当天 |
| `daysPerArticle` | Int | 2 |
| `studyWeekdays` | Set\<DayOfWeek\> | 固定全选七天。字段保留但第一版不开放修改，将来要加休息日从这里进 |
| `timezone` | String | `Asia/Shanghai`（固定） |

### 7.3 历史事件（可选独立表）

勾选、撤销、补打卡、重排各产生一条。撤销事件必须留痕（记录被删了，但"我撤销过"这件事要留在历史里）——这是原型的做法，我保留。

| 字段 | 类型 |
|---|---|
| `id` / `at` / `type` / `articleIndex?` / `taskIndex?` / `note` | Long / Instant / Enum / Int? / Int? / String |

## 8. 异常与边界

| 情况 | 处理 |
|---|---|
| 起点设为 42（全部学完） | 直接进完成页 |
| 起点设为 0 | 从第 1 篇开始，正常 |
| 计划开始日期设在未来 | 今天不属于任何计划日，today 显示"计划将于 X 月 X 日开始"，不算逾期，可以提前做 |
| 计划开始日期设在过去 | 立刻按公式计算出缺额，正常显示暗铜卡 |
| 用户在同一秒重复点同一项 | 记录 id 唯一，第二次是幂等的 no-op |
| 撤销后再勾同一项 | 生成新记录，时间戳是新的 |
| 补打卡到未来日期 | 不允许，面板里未来日期只能"提前完成"（记为当下时刻），不能选未来时间戳 |
| 设备时间被改 | 一律信设备时钟转北京时区；不做防作弊 |
| 跨天时正开着 App | 到北京零点自动重算（R45） |
| 系统字体放大 130%+ | 日历格隐藏「篇·第几天」小字、任务行转两行 |
| 系统字体放大 200% | 保证不崩不截断，允许布局变松（设计没覆盖，实现兜底） |
| 屏幕宽 360dp | 日历格高 48dp，gap 保持 4dp |
| 数据库损坏/迁移失败 | 第一版：捕获后提示"数据读取失败"，不静默清库 |
| 全部 252 项完成后又撤销一项 | 从完成页退回 today，完整篇数 −1 |
| 重排后额度为 0，日历上过去的金环 | 保持不变（历史事实不因重排改变） |

## 9. 验收标准（第一版整体）

1. 能装到安卓手机上，图标能点开，完全离线可用。
2. 首次进入能走完向导，设成"已学完 12、下一篇 13、今天开始、2 天一篇"。
3. today 显示 `28.57%`、`72 / 252`、`12 / 42 篇`、当前第 13 篇 `0/6`。
4. 勾 6 项后：`78 / 252`、`30.95%`、`13 / 42 篇`、主按钮变"开始第 14 篇"、额度 +1 天。
5. 杀进程重开，上述数据一项不差。
6. 把系统时区改成别的国家，"今天"仍按北京时间判定。
7. 月历上能看到提前完成的金环、今天的外环、缺额的暗铜格。
8. 面板里能给过去某天补记，补完首页缺额同步减少。
9. 撤销弹窗写出的回退数字与撤销后实际数字一致。
10. 重新排期后历史记录的时间戳一条没变。
11. 导出一份备份 → 清掉 App 数据 → 从备份恢复 → 所有数字和历史与导出前完全一致。
12. 核心算法的单元测试全绿，覆盖率不低于 80%。
